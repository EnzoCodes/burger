var express = require("express");
require("../models/burger")(app);

//Create the router for the app, and export the router at the end of your file.

// is importing the same as requiring?

// module.exports = router;
